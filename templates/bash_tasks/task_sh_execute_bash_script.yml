operator: airflow.providers.standard.operators.bash.BashOperator
doc_md: |
    Executa bash scripts que se encontram na pasta scripts.

bash_command: "bash {{ dag.folder }}/{{ dag.dag_id }}/scripts/{{ params.bash_script }}"