operator: airflow.providers.google.cloud.operators.bigquery.BigQueryDeleteDatasetOperator
doc_md: |
  Cria uma task que deleta um dataset existente.

# Projeto no GCP onde o dataset se encontra 
project_id: "{{ params.project_id }}"

# Nome do dataset a ser deletado
dataset_id: "{{ params.dataset_id }}"

# Conexão do Composer com o projeto no GCP
gcp_conn_id: "{{ params.gcp_conn_id }}"

# Condição para que esta task seja executada, com base no estado das tasks anteriores
trigger_rule: all_success


